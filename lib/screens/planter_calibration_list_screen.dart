import 'package:flutter/material.dart';

class PlanterCalibrationListScreen extends StatelessWidget {
  const PlanterCalibrationListScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Calibração Plantadeira')),
      body: Center(child: Text('Tela de lista de calibração em construção')),
    );
  }
}
