import 'package:flutter/material.dart';

class PlanterCalibrationScreen extends StatelessWidget {
  const PlanterCalibrationScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Nova Calibração Plantadeira')),
      body: Center(child: Text('Tela de calibração em construção')),
    );
  }
}
